<%= simple_form_for [@project] do |f| %>
  <%= f.input :name, placeholder: "Mon premier projet", label: "Nom du projet", class: 'mb-3' %>
  <%= f.submit value: "Modifier", class: 'btn btn-primary' %>
<% end %>
